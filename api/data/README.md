### data

## Overview
- directory that contains the migrations directory in which the initialized tables.js is 
- housed. This file contains the tables that will be used for this project that are housed 
- as part of the PostgreSQL db used to store Persistence data.
- It also carries the dbConfig file which allows queries to be formed in Javascript and 
- converted into PostgreSQL queries that will be executed in the database.